import styled from "styled-components";

const Container = styled.div`
  width: 100%;
  background-color: aqua;
`;

export default function NavBar() {
  return <Container>this is navigation bar</Container>;
}
